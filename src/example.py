import warnings
import logging
import asyncio
import aiohttp
print("Hello World YYYYYYYYYY!")
def run_function(symbol: str, api_key: str):
    """Function to run the stock analysis"""
    # create a session
    print (symbol)
    print (api_key)

run_function(symbol="AAPL", api_key="demo")


